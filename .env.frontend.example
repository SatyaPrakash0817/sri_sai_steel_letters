# Frontend build-time variables (Vite)
# Set this to your deployed backend URL (Railway/Render)
VITE_API_BASE_URL=https://your-backend-domain.up.railway.app

# Keep root unless deploying under sub-path
VITE_BASE_PATH=/

# Local development proxy target
VITE_DEV_API_PROXY_TARGET=http://localhost:4000
